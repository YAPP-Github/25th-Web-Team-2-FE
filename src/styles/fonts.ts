import { css } from '@emotion/react';

export const fonts = {
  // body
  body: {
    R16: css`
      font-size: 1.6rem;
      font-style: normal;
      font-weight: 400;
      line-height: 2.4rem;
      letter-spacing: 0%;
    `,
  },
};
